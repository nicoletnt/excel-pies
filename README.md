# Excel Tech Pies (Multi-label)

Creates multi-label technology pie counts (AI, Data Analytics, Big Data, IoT, Mobile Applications, Software, Cloud computing, Other).

## Quick start (local)
```bash
python -m venv .venv
# Windows PowerShell:
. .venv\Scripts\Activate.ps1
pip install -r requirements.txt
python make_pies_multilabel.py